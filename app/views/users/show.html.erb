<h1>
  <h1>
    <%= @user.name.capitalize %>
  </h1>
  <dl>
   
    <dt>
      <strong>Email</strong>
      <dd>
        <%= @user.email %>
      </dd>
    </dt>

    <dt>
      <strong>Admin</strong>
      <dd>
      <% if @user.try(:admin?) %>
      This user is an admin to the system
      <%else%>
      This user is not an admin 

      <%end %>

    
      </dd>
    </dt>

    
    
  </dl>
  <% if current_user.try(:admin?) %>
  <%unless @user.try(:admin)%>
  <%= link_to 'Delete' ,user_path, method: :delete %>
  <br/>
  <%end%>
  <%end%>

  <%= link_to 'Back' ,:back %>
